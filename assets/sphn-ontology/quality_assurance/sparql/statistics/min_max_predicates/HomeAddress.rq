PREFIX sphn:<https://biomedit.ch/rdf/sphn-schema/sphn#>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT *
WHERE {
    {
    SELECT ?origin (MIN(?value) as ?min) (MAX(?value) as ?max)
    WHERE {
        ?resource a <https://biomedit.ch/rdf/sphn-schema/sphn#HomeAddress> .

        BIND("sphn:hasSwissSocioEconomicPosition/sphn:hasValue" as ?origin)
        optional{ ?resource sphn:hasSwissSocioEconomicPosition/sphn:hasValue ?value . }

    }
    GROUP BY ?origin
    }
        UNION
    {
    SELECT ?origin (MIN(?value) as ?min) (MAX(?value) as ?max)
    WHERE {
        ?resource a <https://biomedit.ch/rdf/sphn-schema/sphn#HomeAddress> .

        BIND("sphn:hasSwissSocioEconomicPosition/sphn:hasDistance/sphn:hasValue" as ?origin)
        optional{ ?resource sphn:hasSwissSocioEconomicPosition/sphn:hasDistance/sphn:hasValue ?value . }

    }
    GROUP BY ?origin
    }
        UNION
    {
    SELECT ?origin (MIN(?value) as ?min) (MAX(?value) as ?max)
    WHERE {
        ?resource a <https://biomedit.ch/rdf/sphn-schema/sphn#HomeAddress> .

        BIND("sphn:hasStartDateTime" as ?origin)
        optional{ ?resource sphn:hasStartDateTime ?value . }

    }
    GROUP BY ?origin
    }
        UNION
    {
    SELECT ?origin (MIN(?value) as ?min) (MAX(?value) as ?max)
    WHERE {
        ?resource a <https://biomedit.ch/rdf/sphn-schema/sphn#HomeAddress> .

        BIND("sphn:hasEndDateTime" as ?origin)
        optional{ ?resource sphn:hasEndDateTime ?value . }

    }
    GROUP BY ?origin
    }

}